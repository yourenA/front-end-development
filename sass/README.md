# sass

```
全局安装sass.如果你使用的是 Windows， 就需要先安装 Ruby。
$ npm install -g sass

将scss 转换为css ,--watch 监视scss文件的改动并更新 CSS 
$ sass --watch input.scss output.css

监视整个目录
$ sass --watch app/sass:public/stylesheets
```